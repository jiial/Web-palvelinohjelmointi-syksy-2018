<p id="notice"><%= notice %></p>

<h2>
  <%= @brewery.name %>
</h2>

<i>
  Established in 
  <%= @brewery.year %>
</i>

<p>
  Number of beers:
  <%= @brewery.beers.count %>
</p>

<p>
  <% @brewery.beers.each do |beer| %>
    <%= link_to beer.name, beer %>
  <% end %>
</p>

<% if @brewery.ratings.empty? %>
<% else %>
  Has <%= @brewery.ratings.length %> <%= "rating".pluralize(@brewery.ratings.length) %>, average <%= @brewery.average_rating %>
<% end %>

<%= link_to('Edit', edit_brewery_path(@brewery)) if current_user %> |
<%= link_to 'Back', breweries_path %>
