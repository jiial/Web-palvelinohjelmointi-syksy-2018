<%= form_with(model: beer_club, local: true) do |form| %>
  <% if beer_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
      <% beer_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, "Name" %>
    <%= form.text_field :name, class: "form-control", placeholder: "Enter name for beer club" %>
  </div>

  <div class="form-group">
    <%= form.label :founded, "Founded" %>
    <%= form.text_field :founded, class: "form-control", placeholder: "Give founding year" %>
  </div>

  <div class="form-group">
    <%= form.label :city, "City" %>
    <%= form.text_field :city, class: "form-control", placeholder: "Enter city" %>
  </div>

  <button type="submit" class="btn btn-primary">Create beer club</button>
<% end %>
